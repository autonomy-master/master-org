# Credentials / secrets — never commit
.discord-webhooks.json
.env
.env.*
*.pem
*.key

# Per-developer Claude Code config (not for sharing)
.claude/settings.local.json
.claude/.credentials*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Logs
*.log

# Local-only metrics history (raw API dumps, accumulated by loops)
.company/**/.metrics-history.jsonl
.company/**/.cache/
.company/**/.state.*
